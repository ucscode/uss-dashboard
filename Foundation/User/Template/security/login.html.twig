{% extends '@Foundation/User/Template/security/scheme/layout.html.twig' %}

{% import '@Foundation/User/Template/security/scheme/macros.lib.twig' as macros %}

{% block contentMain %}

    {{ form.export|raw }}

{% endblock %}

{% block contentMainAfter %}

	<hr/>
	
	{{ macros.callToAction(
		{
			question: 'Forgot Password?',
			text: 'Reset Password',
			url: __dashboard.getDocument('recovery').url,
		}
	) }}

	{{ macros.callToAction(
		{
			question: "Don't have an account?",
			text: 'Create an account',
			url: __dashboard.getDocument('register').url,
		}
	) }}

{% endblock %}