<div class="profile-box ml-1">
    <button class="dropdown-toggle bg-transparent border-0" type="button" id="profile" data-bs-toggle="dropdown" aria-expanded="false">
        <div class="profile-info">
            <div class="info">
                <div class="image">
                    <img src="{{ user.avatar }}" alt=""/>
                    <span class="status"></span>
                </div>
            </div>
        </div>
        <i class="bi bi-chevron-down"></i>
    </button>
    
    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="profile">
        <li class='mb-1'>
            <div class='px-3 py-2 text-end bg-light fs-12px'>
                <span class='text-success'>
                    {{ user.username ? user.username : user.email }}
                </span>
                <i class='bi bi-info-circle ms-1'></i>
            </div>
        </li>

        {% for item in __dashboard.props.userMenu.children %}
            <li>
                <a href="{{ item.getAttribute('href') }}" target="{{ item.getAttribute('target') }}"> 
                    <i class="{{ item.getAttribute('icon') ?? "bi bi-question-circle" }}"></i>
                    {{ item.getAttribute('label') }} 
                </a>
            </li>
        {% endfor %} 
    </ul>
</div>
