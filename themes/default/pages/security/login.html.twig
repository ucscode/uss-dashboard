{% extends _theme ~ '/security/layout.html.twig' %}

{% block main_right_form_element %}

    {{ form.getHTML(1)|raw }} 

	<div class="mt-2">
		<p class="text-sm text-center">
			Forgot Password? 
			<a href="{{ DashboardExtension.getArchiveUrl('recovery') }}" class="hover-underline text-nowrap">Reset Password</a>
		</p>
	</div>

	<div class="mt-2">
		<p class="text-sm text-center">
			Don’t have an account yet? 
			<a href="{{ DashboardExtension.getArchiveUrl('register') }}" class='text-nowrap'>Create an account</a>
		</p>
	</div>

{% endblock %}